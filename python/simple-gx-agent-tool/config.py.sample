from settings import AgentSettings, ServiceSettings


agent_settings = AgentSettings(
    api_key=OPENAI_API_KEY,
)

groundx_settings = ServiceSettings(
    api_key=GROUNDX_API_KEY,
    bucket_id=BUCKET_ID,
)